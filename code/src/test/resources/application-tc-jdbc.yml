spring:
  datasource:
    url: jdbc:tc:postgresql:14-alpine:///testdb
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

testcontainers:
  enabled: true
